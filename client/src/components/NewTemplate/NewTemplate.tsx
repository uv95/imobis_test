import style from './NewTemplate.module.scss';
    
interface NewTemplateProps {
 
}
    
export const NewTemplate = ({  }: NewTemplateProps) => {
    return (
        <div className={style.NewTemplate}>
            NewTemplate
        </div>
    );
};